// Kept for backwards-compatibility. The authenticated clients now live in
// ./supabase/server (Server Components / Actions) and ./supabase/client (browser).
export { createClient, isConfigured } from "./supabase/server";
